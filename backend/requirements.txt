# ShotFlow — 后端运行时依赖
# Python 3.12+

# Web 框架
# fastapi 需 >=0.139 才能兼容 starlette 1.x；fastmcp-slim[server] 的 MCP 服务器依赖
# starlette>=1.0.1（mcp 包要求），因此必须同步升级 fastapi/starlette/sse-starlette，
# 否则 REST 与 MCP 两条路径依赖互斥（旧 0.118/0.45.3 组合下 MCP 服务器 import 不了 mcp）。
fastapi==0.139.0
starlette==1.3.1
uvicorn[standard]==0.50.2
python-multipart==0.0.32
sse-starlette==3.4.5

# 数据库
SQLAlchemy==2.0.51
alembic==1.18.5
# 驱动按部署环境选择：
#   生产(PostgreSQL): psycopg[binary]>=3.2.0（注意 3.1.18 在 PyPI 不存在，用 3.2.x）
#   开发(SQLite):     无需额外驱动
psycopg[binary]>=3.2.0
# 异步驱动（Phase D 接 PostgreSQL async 用）
asyncpg>=0.29.0

# MCP 暴露（让外部智能体零改造调用 ShotFlow 出片）
# 必须用 fastmcp-slim 真实实现包（不要写 fastmcp==3.4.4 元包）。
# 必须带 [server] extra 才能 import `from fastmcp import FastMCP`（拉入 mcp 包），
# 否则 mcp_server.py 启动报 ModuleNotFoundError: No module named 'mcp'。
# 已升级 fastapi/starlette 至 1.x 兼容线，[server] extra 的 starlette>=1.0.1 不再冲突。
fastmcp-slim[server]==3.4.4
# 腾讯云 SDK（混元生图/视频/TTS 真实调用，SIMULATE 模式不依赖）
tencentcloud-sdk-python>=3.0.0

# 配置与校验
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv>=1.1.0
# EmailStr 类型校验依赖（pydantic 不自带）
email-validator>=2.3.0

# 安全
# 直接使用 bcrypt 库（passlib 1.7.x 与 bcrypt 4.x 存在兼容问题）
bcrypt==4.3.0
# PyJWT 是 Python JWT 事实标准，维护活跃；python-jose 维护停滞且有历史
# 算法混淆 CVE（CVE-2024-33663/33664/33665），改用 PyJWT 规避。
PyJWT==2.10.1

# 任务队列
celery[redis]==5.6.3
redis==5.3.1

# HTTP / 工具
httpx==0.28.1
PyYAML==6.0.3

# Edge TTS（免费微软 TTS + WordBoundary 精确字幕时间轴）
edge-tts>=7.0.0

# 复用现有生成脚本所需的运行时依赖（与 08_Automation/requirements.txt 对齐）
requests==2.34.2
