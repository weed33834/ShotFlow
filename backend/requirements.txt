# ShotFlow — 后端运行时依赖
# Python 3.12+

# Web 框架
fastapi==0.110.0
uvicorn[standard]==0.50.2
python-multipart==0.0.32
sse-starlette==2.4.1

# 数据库
SQLAlchemy==2.0.51
alembic==1.18.5
# 驱动按部署环境选择：
#   生产(PostgreSQL): psycopg[binary]==3.1.18
#   开发(SQLite):     无需额外驱动
psycopg[binary]==3.1.18

# 配置与校验
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.0.1
# EmailStr 类型校验依赖（pydantic 不自带）
email-validator>=2.3.0

# 安全
# 直接使用 bcrypt 库（passlib 1.7.x 与 bcrypt 4.x 存在兼容问题）
bcrypt==4.3.0
# 3.x security fixes
python-jose[cryptography]==3.5.0

# 任务队列
celery[redis]==5.6.3
redis==5.3.1

# HTTP / 工具
httpx==0.27.0
PyYAML==6.0.3

# 复用现有生成脚本所需的运行时依赖（与 08_Automation/requirements.txt 对齐）
requests==2.34.2
